# Meetup tg bot
Данный репозиторий представляет собой удобный инструмент для управления и планированию мероприятий.
Содержит в себе веб админку, а так же бота, через который можно отправлять вопрсы спикерам, а лекторам соотвесвенно их просматривать и отвечать.


---
# 🔐 Переменные окружения

Образ с Django считывает настройки из переменных окружения.  
Создайте файл `.env` в корневом каталоге и запишите туда данные в формате: `ПЕРЕМЕННАЯ=значение`

- **`SECRET_KEY`** — обязательная секретная настройка Django.  
  Это соль для генерации хэшей. Значение может быть любым, важно лишь, чтобы оно никому не было известно.  
  [Документация Django](https://docs.djangoproject.com/en/3.2/ref/settings/#secret-key)
- **`DEBUG`** — настройка Django для включения отладочного режима.  
  Принимает значения `TRUE` или `FALSE`.  
  [Документация Django](https://docs.djangoproject.com/en/3.2/ref/settings/#std:setting-DEBUG)
- **`ALLOWED_HOSTS`** — настройка Django со списком разрешённых адресов.  
  Если запрос прилетит на другой адрес, то сайт ответит ошибкой **400 Bad Request**.  
  Можно перечислить несколько адресов через запятую, например: `127.0.0.1,192.168.0.1,site.test`  
  [Документация Django](https://docs.djangoproject.com/en/3.2/ref/settings/#allowed-hosts)
- **`DATABASE_URL`** — адрес для подключения к базе данных PostgreSQL.  
  Другие СУБД не поддерживаются.  
  [Формат записи](https://github.com/jacobian/dj-database-url#url-schema)
- **`TELEGRAM_BOT_TOKEN`** — токен Telegram-бота, полученный от @BotFather.  
  Используется для запуска и авторизации бота в API Telegram.  
  [Документация Telegram](https://core.telegram.org/bots/faq#how-do-i-create-a-bot)
- **`YOOKASSA_SHOP_ID`** — идентификатор магазина.  
  Данные ЮKassa для приёма донатов через бота.  
  [Документация Юkassa](https://yookassa.ru/developers/using-api/interaction-format)
- **`YOOKASSA_SECRET_KEY`** — Секретный ключ.  
  Данные ЮKassa для приёма донатов через бота.  
  [Документация Юkassa](https://yookassa.ru/developers/using-api/interaction-format)
- **`YOOKASSA_RETURN_URL`** — куда возвращать пользователя после оплаты (например, `https://t.me/yourbot`).  


# Запустить сайт для локальной разработки
## 🐳 Запуск локально в Doker

>
>📁 Каталог local_deployment предназначен для локального запуска и отладки проекта в контейнерах Docker.
>
>В нём находятся необходимые файлы docker-compose-dev.yaml и связанные конфигурации для быстрого старта.


1. **Подготовить окружение к локальной разработке:**

    Для запуска необходим установленный [Docker Desktop](https://www.docker.com/get-started/).
2. **Запустить базу данных и сайт:**
    ```shell
    cd local_deployment # перейти в каталог Compose-файлом
    docker compose -f docker-compose-dev.yml up --build # собрать образ
    ```
 
3. **Создать суперпользователя (*в отдельном терминале*):**
    Перейти в каталог с `docker-compose`:
    ```shell
    cd local_deployment # перейти в каталог Compose-файлом
    docker compose -f docker-compose-dev.yml exec web sh -lc 'python manage.py createsuperuser' # создаём в БД учётку суперпользователя
    ```
    
    После запуска сайт будет доступен по адресу: [http://localhost:8000/](http://localhost:8000/).

    Админка - [http://localhost:8000/admin/](http://127.0.0.1:8000/admin/).

## 🐳 Развертывание на сервере prod-версии

>
>📁 Каталог production_deployment предназначен для запуска проекта на сервере в контейнерах Docker.
>
>В нём находятся необходимые файлы docker-compose-prod.yaml и связанные конфигурации для быстрого старта.


1. **Скопируйте файл `production_deployment/deploy.sh` и `.env` в папку на сервере (например opt).**
2. **Запустите Bash скрипт деплоя:**
    ```bash
       ./deploy.sh
    ```
   Если у файла нет прав на исполнение выполните команду:
    ```
    chmod +x deploy.sh
    ```    
3. **Создать административную учетную запись:**
   - Перейдите в каталог проекта:
      ```sh
      cd dvmn_meetuptg_bot/production_deployment
      ```
  - запустите команду:

      ```sh
      docker compose -f docker-compose-prod.yml exec web sh -lc 'python manage.py createsuperuser'
      ```
4. **Настройте Nginx указав пути к staticfiles и media** (с учетом расположения проекта `opt/dvmn_meetuptg_bot/meetup_tg_bot/`):

`opt/dvmn_meetuptg_bot/meetup_tg_bot/staticfiles/`

`opt/dvmn_meetuptg_bot/meetup_tg_bot/media/`

 


